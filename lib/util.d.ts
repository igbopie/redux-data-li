/**
 * Store into the TypeCache and return the type name.
 * @throws Will throw an error if the name is already defined.
 *
 * @param {String} label Action type name
 * @return {String} label
 */
export declare function type(label: string): string;
